---
import Base from '@/layouts/Base.astro';
---

<Base title="Home">
  <div class="hero">
    <div class="hero-ornament">&#10087;</div>
    <h1>Works of St. Thomas Aquinas</h1>
    <p class="subtitle">Latin texts with English translations</p>
    <div class="hero-flourish">
      <span class="flourish-left">&#10087;</span>
      <span class="flourish-center">AD MAIOREM DEI GLORIAM</span>
      <span class="flourish-right">&#10087;</span>
    </div>
  </div>

  <section class="intro">
    <p>
      This collection presents the writings of <strong>St. Thomas Aquinas</strong> (1225-1274),
      the Dominican friar, philosopher, and theologian whose works have profoundly
      shaped Catholic theology and Western philosophy.
    </p>
    <p>
      Originally compiled by <a href="http://www.dhspriory.org/kenny">Fr. Joseph Kenny, O.P.</a> (1936-2013),
      this archive provides access to Latin texts alongside English translations.
    </p>
    <div class="intro-cta">
      <a href="/works/" class="btn-primary">Browse All Works</a>
    </div>
  </section>

  <div class="divider">
    <span class="divider-ornament">&#10070;</span>
  </div>

  <section class="categories">
    <h2>Browse by Category</h2>

    <div class="category-grid">
      <a href="/works/?category=major-theological" class="category-card">
        <div class="card-icon">&#128220;</div>
        <h3>Major Theological Works</h3>
        <p>Summa Theologica, Summa Contra Gentiles</p>
        <span class="card-arrow">&#8594;</span>
      </a>

      <a href="/works/?category=quaestiones-disputatae" class="category-card">
        <div class="card-icon">&#10068;</div>
        <h3>Quaestiones Disputatae</h3>
        <p>De veritate, De potentia, De malo</p>
        <span class="card-arrow">&#8594;</span>
      </a>

      <a href="/works/?category=opuscula" class="category-card">
        <div class="card-icon">&#128214;</div>
        <h3>Opuscula</h3>
        <p>De ente et essentia, De regno</p>
        <span class="card-arrow">&#8594;</span>
      </a>

      <a href="/works/?category=aristotle-commentary" class="category-card">
        <div class="card-icon">&#127981;</div>
        <h3>Aristotle Commentaries</h3>
        <p>Physics, Ethics, Metaphysics</p>
        <span class="card-arrow">&#8594;</span>
      </a>

      <a href="/works/?category=biblical-commentary" class="category-card">
        <div class="card-icon">&#10013;</div>
        <h3>Biblical Commentaries</h3>
        <p>Psalms, Job, Gospels, Epistles</p>
        <span class="card-arrow">&#8594;</span>
      </a>

      <a href="/works/?category=popular" class="category-card">
        <div class="card-icon">&#128591;</div>
        <h3>Popular Works</h3>
        <p>Creed, Our Father, Prayers</p>
        <span class="card-arrow">&#8594;</span>
      </a>
    </div>
  </section>

  <div class="divider">
    <span class="divider-ornament">&#10070;</span>
  </div>

  <section class="quick-links">
    <h2>Quick Access</h2>
    <div class="links-grid">
      <a href="/thomas/summa/index.html" class="quick-link">
        <span class="link-title">Summa Theologica</span>
        <span class="link-desc">Complete text in Latin & English</span>
      </a>
      <a href="/thomas/ContraGentiles.htm" class="quick-link">
        <span class="link-title">Summa Contra Gentiles</span>
        <span class="link-desc">Latin & English translation</span>
      </a>
      <a href="/thomas/" class="quick-link">
        <span class="link-title">Legacy Index</span>
        <span class="link-desc">Original site navigation</span>
      </a>
    </div>
  </section>

  <style>
    .hero {
      text-align: center;
      padding: 3rem 0 2.5rem;
      margin-bottom: 2.5rem;
    }

    .hero-ornament {
      font-size: 2rem;
      color: var(--color-accent);
      margin-bottom: 1rem;
      opacity: 0.6;
    }

    .hero h1 {
      font-size: 2.75rem;
      margin-bottom: 0.5rem;
      letter-spacing: 0.06em;
    }

    @media (max-width: 600px) {
      .hero h1 {
        font-size: 2rem;
      }
    }

    .subtitle {
      color: var(--color-text-muted);
      font-style: italic;
      font-size: 1.2rem;
      margin-bottom: 1.5rem;
    }

    .hero-flourish {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 1rem;
      font-family: var(--font-heading);
      font-size: 0.75rem;
      letter-spacing: 0.15em;
      color: var(--color-text-muted);
      text-transform: uppercase;
    }

    .flourish-left,
    .flourish-right {
      color: var(--color-accent);
      opacity: 0.5;
    }

    .flourish-right {
      transform: scaleX(-1);
    }

    .intro {
      max-width: 600px;
      margin: 0 auto 2rem;
      text-align: center;
    }

    .intro p {
      max-width: none;
    }

    .intro-cta {
      margin-top: 1.5rem;
    }

    .btn-primary {
      display: inline-block;
      font-family: var(--font-heading);
      font-size: 0.9rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      padding: 0.875rem 2rem;
      background-color: var(--color-accent);
      color: #ffffff;
      border: 2px solid var(--color-accent);
      border-radius: 2px;
      text-decoration: none;
      transition: all var(--transition-normal);
    }

    .btn-primary:hover {
      background-color: transparent;
      color: var(--color-accent);
      text-decoration: none;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px var(--color-shadow);
    }

    .categories {
      margin-bottom: 2rem;
    }

    .categories h2 {
      text-align: center;
      margin-bottom: 1.5rem;
    }

    .category-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1.25rem;
    }

    .category-card {
      display: block;
      position: relative;
      padding: 1.5rem;
      background-color: var(--color-bg-card);
      border: 1px solid var(--color-border);
      border-radius: 4px;
      text-decoration: none;
      transition: all var(--transition-normal);
      overflow: hidden;
    }

    .category-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 3px;
      background-color: var(--color-accent);
      transform: scaleX(0);
      transition: transform var(--transition-normal);
    }

    .category-card:hover {
      border-color: var(--color-accent);
      text-decoration: none;
      transform: translateY(-3px);
      box-shadow: 0 8px 24px var(--color-shadow);
    }

    .category-card:hover::before {
      transform: scaleX(1);
    }

    .category-card:hover .card-arrow {
      opacity: 1;
      transform: translateX(0);
    }

    .category-card:hover h3 {
      color: var(--color-accent);
    }

    .card-icon {
      font-size: 1.75rem;
      margin-bottom: 0.75rem;
      opacity: 0.7;
    }

    .category-card h3 {
      font-size: 1.1rem;
      margin-bottom: 0.35rem;
      color: var(--color-text);
      transition: color var(--transition-fast);
    }

    .category-card p {
      font-size: 0.9rem;
      color: var(--color-text-muted);
      margin-bottom: 0;
      line-height: 1.5;
    }

    .card-arrow {
      position: absolute;
      bottom: 1.25rem;
      right: 1.25rem;
      font-size: 1.25rem;
      color: var(--color-accent);
      opacity: 0;
      transform: translateX(-8px);
      transition: all var(--transition-normal);
    }

    .quick-links {
      margin-bottom: 1rem;
    }

    .quick-links h2 {
      text-align: center;
      margin-bottom: 1.5rem;
    }

    .links-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1rem;
    }

    .quick-link {
      display: flex;
      flex-direction: column;
      padding: 1.25rem;
      background-color: var(--color-highlight);
      border: 1px solid var(--color-border-light);
      border-radius: 4px;
      text-decoration: none;
      text-align: center;
      transition: all var(--transition-normal);
    }

    .quick-link:hover {
      border-color: var(--color-accent);
      text-decoration: none;
      transform: translateY(-2px);
      box-shadow: 0 4px 12px var(--color-shadow);
    }

    .link-title {
      font-family: var(--font-heading);
      font-size: 1rem;
      color: var(--color-accent);
      margin-bottom: 0.25rem;
      letter-spacing: 0.02em;
    }

    .link-desc {
      font-size: 0.85rem;
      color: var(--color-text-muted);
    }
  </style>
</Base>
